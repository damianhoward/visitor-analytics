name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

# CodeQL and dependency-review are absent by constraint, not oversight: both need GitHub
# Advanced Security on a private repository. OWASP dependency-check covers the dependency
# side locally (./gradlew dependencyCheckAnalyze).
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check for Claude attribution in history
        run: |
          if git log --all --format="%B" | grep -iE "co-authored-by:[[:space:]]*claude|noreply@anthropic|generated (with|by)[[:space:]]*claude|🤖"; then
            echo "::error::Claude attribution found in git history — see CLAUDE.md commit standards."
            exit 1
          fi

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Set up Node # Prettier (web + config/docs formatting) runs under Spotless
        uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Spotless check
        run: ./gradlew --no-daemon spotlessCheck

      - name: Build, test, and enforce coverage
        run: ./gradlew --no-daemon clean check

      - name: Publish test report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: build/reports/tests/test
          if-no-files-found: ignore
